# Mapbox public access token (pk.*). Get one at https://account.mapbox.com/access-tokens/
# Copy this file to .env and fill in your token. Anything prefixed EXPO_PUBLIC_ is
# inlined into the client bundle by Expo at build time.
EXPO_PUBLIC_MAPBOX_TOKEN=your_mapbox_public_token_here
